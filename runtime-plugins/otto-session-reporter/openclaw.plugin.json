{
  "id": "otto-session-reporter",
  "name": "Otto Session Reporter",
  "description": "Reports session lifecycle and transcript data to the Otto workspace app.",
  "contracts": {},
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "timeoutMs": {
        "type": "integer",
        "minimum": 1000
      },
      "debounceMs": {
        "type": "integer",
        "minimum": 500
      }
    }
  }
}

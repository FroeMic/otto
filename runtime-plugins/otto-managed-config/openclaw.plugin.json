{
  "id": "otto-managed-config",
  "name": "Otto Managed Config",
  "description": "Managed instruction file tools backed by the workspace app.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "timeoutMs": {
        "type": "integer",
        "minimum": 1000
      }
    }
  }
}
